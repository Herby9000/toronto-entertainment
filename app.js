let data=[],cat='Music';const grid=document.querySelector('#events'),tpl=document.querySelector('#card'),summary=document.querySelector('#summary'),aud=document.querySelector('#audience');const fmt=new Intl.DateTimeFormat('en-CA',{month:'short',timeZone:'America/Toronto'});function render(){const a=aud.value,x=data.filter(e=>e.category===cat&&(a==='all'||e.audience===a||e.audience==='all'));grid.innerHTML='';summary.textContent=`${x.length} ${cat.toLowerCase()} pick${x.length===1?'':'s'} on the calendar`;x.forEach(e=>{const n=tpl.content.cloneNode(true),d=new Date(e.date+'T12:00:00');n.querySelector('.date b').textContent=d.getDate();n.querySelector('.date span').textContent=fmt.format(d);n.querySelector('.tag').textContent=e.audience==='date'?'Date night':e.audience==='family'?'Family friendly':e.category;n.querySelector('h2').textContent=e.title;n.querySelector('.venue').textContent=e.venue;n.querySelector('.desc').textContent=e.description;n.querySelector('a').href=e.url;grid.append(n)});if(!x.length)grid.innerHTML='<article><div></div><div class="copy"><span class="tag">Fresh list pending</span><h2>Nothing confirmed yet.</h2><p class="desc">We only publish events our updater can verify at an official source. Check back after the next weekly refresh.</p></div></article>'}document.querySelectorAll('.tabs button').forEach(b=>b.onclick=()=>{cat=b.dataset.cat;document.querySelectorAll('.tabs button').forEach(x=>x.classList.toggle('active',x===b));render()});aud.onchange=render;fetch('data/events.json').then(r=>{if(!r.ok)throw Error();return r.json()}).then(j=>{data=j.events;document.querySelector('#updated').textContent='Updated '+new Date(j.updated).toLocaleDateString('en-CA');document.querySelector('.tabs button').click()}).catch(()=>summary.textContent='The listings could not be loaded. Please try again.')